node_modules/
dist/
.env
.dev.vars
.netlify/
*.log
playwright-report/
test-results/
.claude/
!.claude/commands/
!.claude/commands/*.md
!.claude/skills/
!.claude/skills/*/
!.claude/skills/*/SKILL.md
coverage/
UsersjschrAppDataLocalGoogleChromeUser Data/

# Build artifacts — contain internal URLs, DSN keys, performance data
lighthouse-report.json
e2e/sync-proof-report/
browserstack-report/
scripts/sa_key_temp.json
scripts/backfill-run.cjs

# Android release keystore — never commit; back up to Dropbox / 1Password
*.jks
*.keystore
android/local.properties
scripts/keystore-credentials.txt

# Capacitor iOS build output — not source, fully regenerable.
# When resuming iOS development: npm run build && npx cap add ios && npx cap sync
ios/
