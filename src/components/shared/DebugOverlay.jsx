/**
 * DebugOverlay — DISABLED. Tablet TTS/mascot issues resolved as of v2.2.0.
 * Kept as empty export; renders nothing. Safe to import if referenced anywhere.
 */
export default function DebugOverlay() { return null; }
