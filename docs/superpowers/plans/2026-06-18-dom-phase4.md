# Phase 4 — Dom (Home) Redesign Implementation Plan

> **For agentic workers:** execute task-by-task; each task ends with a verified, committed deliverable. Steps use checkbox (`- [ ]`).

**Goal:** Turn Home into the host-family welcome ("Dom"): a host-of-the-day greets the user over an Adriatic scene, with quiet gold-dot progress — plus the deferred app-wide header carrying the "Me" avatar, and the bottom bar reduced to the true 5 places (drop "Ja").

**Architecture:** Evolve `HomeTab` (keep `SessionCard` + `TodaysDiscoveries` intact). Add `HostFamilyWelcome` (composes the committed `CharacterPortrait` cast + a real scene asset). Add a slim app-wide `AppHeader` (brand + Me avatar) mounted in `App.tsx`; remove `profile` from `TabBar`/`Sidebar`; route Me via the header.

**Tech:** React + Vite + TS, plain CSS tokens, Vitest + @testing-library/react, Playwright e2e.

## Global Constraints
- Plain CSS + tokens in `src/index.css`; no Tailwind. Fonts: Outfit (UI) + Playfair (headings).
- Palette: teal `#0e7490`, Croatian red `#D40030`/`#c0392b`, terracotta `#c2410c`, stone `#f5f0e8`, gold `#C8980A`.
- ASCII-only commit messages (CF Pages); end with `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Do NOT push to master; work on `feat/uxui-dom-phase4`. Merging deploys (CF Pages).
- Never break user progress. Keep `home.spec`'s greeting assertion working (retain "Dobro jutro/Dobar dan/Dobra večer, <name>" text).
- Accessible names: the header Me button gets `aria-label="Me"` + `data-testid="header-profile"`; profile must stay reachable after removal from the bar.
- Verify every visual change with a Playwright screenshot before claiming done; rebuild dist before e2e.
- Header MUST respect `env(safe-area-inset-top)` and not overlap screen content (add top spacing).

## File Structure
- Create `src/components/home/hostFamily.ts` — host-of-the-day rotation, per-host Croatian greeting note + English echo, host→scene map. One responsibility: the family welcome data.
- Create `src/components/home/HostFamilyWelcome.tsx` (+ `.test.tsx`) — the welcome hero (scene + host portrait + greeting + gold dots).
- Modify `src/components/home/HomeTab.tsx` — replace the inline greeting (lines ~500–516) with `<HostFamilyWelcome>`.
- Create `src/components/shared/AppHeader.tsx` (+ `.test.tsx`) — slim top bar: brand left, Me avatar right.
- Modify `src/App.tsx` — mount `<AppHeader>` inside `<main>`; pass `setTab`, `name`.
- Modify `src/index.css` — header styles + content top-offset + safe-area.
- Modify `src/components/shared/TabBar.tsx` + `Sidebar.tsx` — remove the `profile` entry (5-place).
- Modify e2e specs that reach Me/profile via the bottom tab → use the header avatar (`data-testid="header-profile"`).

---

## Task 1: hostFamily.ts (rotation + content)
**Files:** Create `src/components/home/hostFamily.ts`, `src/components/home/hostFamily.test.ts`
**Interfaces:** Produces `hostOfDay(dayIdx: number): CharacterName`; `HOST_WELCOME: Record<CharacterName, { hr: string; en: string; scene: string }>` (scene = filename under `public/images/scenes/`).

- [ ] Step 1: Failing test — `hostOfDay(0)`..`hostOfDay(4)` return 5 distinct cast names; `hostOfDay(5) === hostOfDay(0)`; every `CharacterName` has a `HOST_WELCOME` entry with non-empty `hr`/`en` and a `scene`.
- [ ] Step 2: Run red.
- [ ] Step 3: Implement: `ALL_CHARACTERS[((dayIdx % 5) + 5) % 5]`; author the 5 welcome notes (Croatian + English) and host→scene map (baka→dubrovnik-ai.webp, marko→dalmatian-coast.webp, ana→mostar.webp, kovac→plitvice.webp, ivo→zagreb.webp — adjust to existing files).
- [ ] Step 4: Run green.
- [ ] Step 5: Commit `feat(home): add host-of-the-day rotation + welcome content`.

## Task 2: HostFamilyWelcome component
**Files:** Create `src/components/home/HostFamilyWelcome.tsx` (+ test)
**Interfaces:** Consumes `hostFamily.ts`, `CharacterPortrait`. Props: `{ name: string; streakCount: number; dayIdx: number }`. Renders a photographic hero (scene `background-image`), the host `CharacterPortrait` in a gold ring, the time-of-day greeting ("Dobro jutro/Dobar dan/Dobra večer, <name>"), the host note (hr + en), and a gold-dot streak row ("Nth jutro/morning").

- [ ] Step 1: Failing test — renders the host portrait (`getByTestId('portrait-<host>')`), contains the greeting text with the name, renders `streakCount` gold dots (cap e.g. 7), and shows the host note.
- [ ] Step 2: Red.
- [ ] Step 3: Implement (flag stripe + scene + scrim + portrait + greeting + note + dots). Scene via `import.meta.env.BASE_URL + 'images/scenes/' + scene` or `/images/scenes/<scene>`.
- [ ] Step 4: Green + typecheck.
- [ ] Step 5: Commit `feat(home): add HostFamilyWelcome hero`.

## Task 3: Integrate into HomeTab
**Files:** Modify `src/components/home/HomeTab.tsx`
- [ ] Replace the inline greeting block (~500–516) with `<HostFamilyWelcome name={authUser?.d || 'Learner'} streakCount={streak.count} dayIdx={currentDayIdx} />` (only when `authUser`). Keep `SessionCard` + `TodaysDiscoveries`.
- [ ] Verify: typecheck + run `home.spec` (greeting still asserts) locally after build; visual screenshot of Home.
- [ ] Commit `feat(home): render HostFamilyWelcome on Dom`.

## Task 4: AppHeader component
**Files:** Create `src/components/shared/AppHeader.tsx` (+ test)
**Interfaces:** Props `{ name?: string; onProfile: () => void }`. Renders a slim bar: šahovnica/brand left ("Naša Hrvatska"), a Me avatar button right (`aria-label="Me"`, `data-testid="header-profile"`, initial or person glyph). Flag-stripe accent; `padding-top: env(safe-area-inset-top)`.
- [ ] Failing test → red → implement → green; assert the Me button has the testid + aria-label and calls `onProfile` on click.
- [ ] Commit `feat(nav): add slim AppHeader with Me avatar`.

## Task 5: Mount AppHeader + content offset
**Files:** Modify `src/App.tsx`, `src/index.css`
- [ ] Mount `<AppHeader name={name} onProfile={() => setTab('profile')} />` at the top of `<main id="main-content">` (gated to `authScreen==='app'` && not welcome/placement, like TabBar).
- [ ] In `index.css`: header `position: sticky; top: 0; z-index: var(--z-sticky)`; add top padding/margin to `.app-content` so content clears the header; respect safe-area.
- [ ] Verify: screenshots of Home + one other tab (no overlap; safe-area OK). typecheck.
- [ ] Commit `feat(nav): mount app-wide header with Me access`.

## Task 6: Drop "Ja" from the bottom bar (5-place)
**Files:** Modify `src/components/shared/TabBar.tsx`, `src/components/shared/Sidebar.tsx`
- [ ] Remove the `profile` entry from both `TABS` arrays → Dom · Učenje · Grad · Razgovor · Hrvatska. Profile now reached only via the header avatar (and Sidebar may keep a profile affordance in its user card).
- [ ] Verify: `testids.smoke` (nav-profile no longer required on the bar — update the smoke test expectation), typecheck, full unit suite.
- [ ] Commit `feat(nav): reduce bottom bar to five places (Me moves to header)`.

## Task 7: e2e/test reconciliation
**Files:** e2e specs that select the Me/profile tab + `src/tests/testids.smoke.test.tsx`
- [ ] Find e2e using `nav-profile` / `name: 'Me'` on the bar (e.g., mobile-signout, authenticated-user-flow "5 tabs", error-boundary tab loop) → route to profile via `getByTestId('header-profile')`.
- [ ] Update `testids.smoke` so it no longer asserts `nav-profile` on TabBar/Sidebar; assert `header-profile` instead.
- [ ] Rebuild dist; run affected e2e on Desktop Chrome locally.
- [ ] Commit `test: reach profile via header avatar after nav reduction`.

## Self-Review
- Greeting text preserved (home.spec). Profile reachable post-removal (header). Header safe-area + no content overlap. `hostOfDay` deterministic + wraps. Scenes referenced match files in `public/images/scenes/`.
